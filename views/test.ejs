<% function gloria(array) {
    let arr = [];
    for (var i = array.length - 1; i >= 0; --i) {
        arr.push(array[i]);
    }
    return arr;
} %>

<% let database = gloria(picture); %>

<%= database[0] %>